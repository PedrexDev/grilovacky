<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8">
  <title>Grilovačky</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-4">
    <h1 class="mb-4">Grilovačky Františka Ředitele</h1>

    <div class="row">
      <% grilovacky.forEach(g => { %>
        <div class="col-md-6 mb-4">
          <div class="card shadow-sm">
            <div class="card-body">
              <h5 class="card-title"><%= g.date %></h5>
              <p class="card-text">
                <strong>Čas:</strong> <%= g.time %><br>
                <strong>Místo:</strong> <%= g.location %><br>
                <strong>Kapacita:</strong> <%= g.capacity %> lidí
              </p>
              <span class="badge bg-<%= g.status === 'Budoucí' ? 'success' : 'secondary' %>">
                <%= g.status %>
              </span>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>