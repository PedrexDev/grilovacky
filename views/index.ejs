<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Grilovačky</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <h1>Grilovačky</h1>
    <div class="row">
      <% grilovacky.forEach(grilovacka => { %>
        <div class="col-md-4">
          <div class="card mb-4">
            <div class="card-body">
              <h5 class="card-title"><%= grilovacka.date %></h5>
              <p class="card-text">
                <strong>Čas:</strong> <%= grilovacka.time %><br>
                <strong>Místo:</strong> <%= grilovacka.location %><br>
                <strong>Kapacita:</strong> <%= grilovacka.capacity %> lidí
              </p>
              <span class="badge bg-<%= grilovacka.status === 'Budoucí' ? 'success' : 'secondary' %>">
                <%= grilovacka.status %>
              </span>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>